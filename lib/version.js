// Build version — bumped every time a new build is generated, so you can confirm which
// build is actually running (check Settings → bottom, or the browser console on load).
// Shared between components/App.jsx (top bar, quarterPlan stamping) and components/Today.jsx
// (brief cache versioning) -- extracted here so neither has to import it from the other.
export const APP_VERSION="2.73.3";
export const APP_BUILD_DATE="2026-09-15";
export const APP_BUILD_TIME="Add term: defaults to current school, auto-searches the real upcoming term";
